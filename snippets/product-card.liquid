{% assign card_style = style | default: 'below' %}
{% assign quick_add_variant = product.selected_or_first_available_variant %}
{% assign color_option = nil %}
{% for option in product.options_with_values %}
  {% assign option_name = option.name | downcase %}
  {% if color_option == nil %}
    {% if option_name contains 'color' or option_name contains 'colour' %}
      {% assign color_option = option %}
    {% endif %}
  {% endif %}
{% endfor %}

<article class="product-card" style="--product-card-ratio: {{ settings.product_card_ratio | default: '1 / 1' }};">
  <div class="product-card__media">
    <a href="{{ product.url }}">
      {% if product.featured_media %}
        {{ product.featured_media | image_url: width: 900 | image_tag: loading: 'lazy', sizes: '(min-width: 990px) 25vw, (min-width: 750px) 50vw, 100vw', widths: '300, 450, 600, 900', alt: product.title }}
      {% else %}
        <div style="aspect-ratio:1/1;background:#f2f2f2;"></div>
      {% endif %}
    </a>

    {% if settings.show_second_image and product.media.size > 1 %}
      <a class="product-card__secondary" href="{{ product.url }}">
        {{ product.media[1] | image_url: width: 900 | image_tag: loading: 'lazy', sizes: '(min-width: 990px) 25vw, (min-width: 750px) 50vw, 100vw', widths: '300, 450, 600, 900', alt: product.title }}
      </a>
    {% endif %}

    <button class="product-card__wishlist" type="button" data-wishlist-toggle="{{ product.handle }}" aria-pressed="false" aria-label="Save {{ product.title | escape }}">
      {% render 'icon-heart' %}
    </button>

    <div class="product-card__badges">
      {% if product.available == false %}
        <span class="badge badge--muted">Sold out</span>
      {% elsif product.compare_at_price > product.price %}
        {% assign sale_percent = product.compare_at_price | minus: product.price | times: 100 | divided_by: product.compare_at_price %}
        <span class="badge badge--sale">Save {{ sale_percent }}%</span>
      {% endif %}
      {% assign product_tag_string = product.tags | join: ',' | downcase | prepend: ',' | append: ',' %}
      {% if product_tag_string contains ',new,' %}
        <span class="badge">New</span>
      {% endif %}
      {% if product_tag_string contains ',bestseller,' or product_tag_string contains ',best-seller,' or product_tag_string contains ',best seller,' %}
        <span class="badge">Best seller</span>
      {% endif %}
      {% if product_tag_string contains ',limited,' or product_tag_string contains ',limited drop,' %}
        <span class="badge">Limited</span>
      {% endif %}
    </div>

    <div class="product-card__quick-actions">
      {% if settings.enable_quick_view %}
        <button class="button button--secondary" type="button" data-quick-view="{{ product.handle }}" data-product-url="{{ product.url }}">Quick view</button>
      {% endif %}
      {% if settings.enable_quick_add %}
        {% if product.variants.size == 1 %}
          <form method="post" action="{{ routes.cart_add_url }}" data-ajax-cart>
            <input type="hidden" name="id" value="{{ quick_add_variant.id }}">
            <button class="button button--primary" type="submit" {% unless quick_add_variant.available %}disabled{% endunless %}>
              {% if quick_add_variant.available %}Quick add{% else %}Sold out{% endif %}
            </button>
          </form>
        {% else %}
          <a class="button button--primary" href="{{ product.url }}">Options</a>
        {% endif %}
      {% endif %}
    </div>

    {% if card_style == 'overlay' %}
      <div class="product-card__overlay-info">
        <div class="stack">
          <strong>{{ product.title }}</strong>
          {% render 'price', product: product %}
        </div>
      </div>
    {% endif %}
  </div>

  {% if card_style == 'below' %}
    <div class="product-card__info">
      <div class="product-card__text">
        {% if settings.show_vendor and product.vendor != blank %}<small class="product-card__vendor">{{ product.vendor }}</small>{% endif %}
        <a class="product-card__title" href="{{ product.url }}">{{ product.title }}</a>
        <div class="product-card__price">
          {% render 'price', product: product %}
        </div>
      </div>
      {% if color_option and color_option.values.size > 0 %}
        <div class="product-card__swatches" aria-label="Available colors">
          {% for value in color_option.values limit: 5 %}
            {% assign swatch_color = value | downcase | replace: ' ', '' %}
            {% if value contains '#' %}
              {% assign swatch_color = value %}
            {% endif %}
            <span class="product-card__swatch" style="--swatch-color: {{ swatch_color }};" title="{{ value | escape }}">
              <span class="visually-hidden">{{ value }}</span>
            </span>
          {% endfor %}
          {% if color_option.values.size > 5 %}
            <span class="product-card__swatch-more">+{{ color_option.values.size | minus: 5 }}</span>
          {% endif %}
        </div>
      {% endif %}
    </div>
  {% endif %}
</article>
