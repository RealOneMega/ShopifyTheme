<section class="section" data-product-handle="{{ product.handle }}">
  <div class="container">
    <div style="display:grid;gap:2rem;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));">
      <div>
        {% for media in product.media %}
          {{ media | media_tag: image_size: '800x', loading: 'lazy' }}
        {% endfor %}
      </div>
      <div class="stack">
        <h1 class="heading">{{ product.title }}</h1>
        {% render 'price', product: product %}
        {% if section.settings.show_inventory and product.available %}
          <p>In stock: {{ product.inventory_quantity }}</p>
        {% endif %}

        <form method="post" action="/cart/add">
          {% if product.has_only_default_variant %}
            <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
          {% else %}
            <label for="VariantSelect">Variant</label>
            <select id="VariantSelect" class="field field--select" name="id">
              {% for variant in product.variants %}
                <option value="{{ variant.id }}" {% if variant == product.selected_or_first_available_variant %}selected{% endif %}>
                  {{ variant.title }} - {{ variant.price | money }}
                </option>
              {% endfor %}
            </select>
          {% endif %}
          <button class="button button--primary" type="submit">Add to cart</button>
        </form>

        <div class="accordion" data-accordion>
          <div class="accordion__item">
            <button class="accordion__button" data-accordion-button aria-expanded="false">Shipping</button>
            <div class="accordion__panel hidden">{{ section.settings.shipping_text }}</div>
          </div>
          <div class="accordion__item">
            <button class="accordion__button" data-accordion-button aria-expanded="false">Returns</button>
            <div class="accordion__panel hidden">{{ section.settings.returns_text }}</div>
          </div>
        </div>
      </div>
    </div>

    <div style="margin-top:3rem;">
      <h2 class="heading">Reviews</h2>
      <div data-review-app>
        <p>{{ section.settings.review_placeholder }}</p>
        <button class="button button--secondary">Write a review</button>
      </div>
    </div>

    <div style="margin-top:3rem;">
      <h2 class="heading">Recommended for you</h2>
      <div class="product-grid">
        {% for product in recommendations.products limit: 4 %}
          {% render 'product-card', product: product, style: 'below' %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Product",
    "name": "{{ product.title | escape }}",
    "image": [
      {% for media in product.media limit: 3 %}"{{ media | image_url: width: 800 }}"{% unless forloop.last %},{% endunless %}{% endfor %}
    ],
    "description": "{{ product.description | strip_html | escape }}",
    "sku": "{{ product.selected_or_first_available_variant.sku }}",
    "brand": {
      "@type": "Brand",
      "name": "{{ product.vendor | escape }}"
    },
    "offers": {
      "@type": "Offer",
      "priceCurrency": "{{ cart.currency.iso_code }}",
      "price": "{{ product.price | money_without_currency }}",
      "availability": "https://schema.org/{% if product.available %}InStock{% else %}OutOfStock{% endif %}",
      "url": "{{ product.url }}"
    }
  }
</script>

{% schema %}
{
  "name": "Main product",
  "settings": [
    {"type": "checkbox", "id": "show_inventory", "label": "Show inventory messaging", "default": true},
    {"type": "textarea", "id": "shipping_text", "label": "Shipping text", "default": "Fast shipping worldwide."},
    {"type": "textarea", "id": "returns_text", "label": "Returns text", "default": "30-day returns on unused items."},
    {"type": "textarea", "id": "review_placeholder", "label": "Review placeholder", "default": "Install your preferred review app to display reviews here."}
  ]
}
{% endschema %}
